we will be using face image registration system provided by amazon using modified email plus a uploaded image as input. To be specific, the email was registered by amazon cognito, lzr1040738182@126.com for example. And once user login, we will pass modified email(change @ to :) lzr1040738182:126.com as email. Also we will ask the user to upload a local image. We pass those two as inputs. 